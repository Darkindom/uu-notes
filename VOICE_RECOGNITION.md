# 语音识别功能说明

## 功能概述

为了方便老人记录宝宝日常，我们添加了语音识别功能。用户可以通过语音输入来快速记录宝宝的吃、睡、拉等日常活动。

## 功能特性

### 1. 语音录音
- 点击首页的"🎤 语音记录"按钮进入录音页面
- 支持最长 60 秒的录音
- 实时显示录音状态和动画效果

### 2. 语音识别
- 使用微信同声传译插件进行语音转文字
- 支持中文普通话识别
- 识别结果可编辑修改

### 3. 智能分类识别
系统会自动根据语音内容识别以下类别：

#### 🍼 吃（food）
- **识别关键词**：喝、吃、奶、母乳、奶粉、辅食、喂
- **子类别**：
  - 母乳（breast）：识别"母乳"
  - 奶粉（formula）：识别"奶粉"
  - 辅食（solid）：识别"辅食"
- **数量识别**：自动识别毫升(ml)或克(g)
- **示例**：
  - "宝宝刚喝了150毫升奶" → 类别：吃，数量：150毫升
  - "喂了母乳" → 类别：吃，子类别：母乳

#### 😴 睡（sleep）
- **识别关键词**：睡、觉、入睡、睡着
- **示例**：
  - "宝宝睡着了" → 类别：睡
  - "刚刚入睡" → 类别：睡

#### 🚽 拉（shit）
- **识别关键词**：拉、大便、尿、换尿布、尿片、尿不湿
- **子类别**：
  - 大便（big）：识别"大便、拉"
  - 换尿布（small）：识别"尿、换尿布、尿片、尿不湿"
- **示例**：
  - "宝宝拉了" → 类别：拉，子类别：大便
  - "刚换了尿布" → 类别：拉，子类别：换尿布

#### ✨ 其他（other）
- 不属于以上类别的其他内容

### 4. 手动输入
- 如果语音识别不可用或失败，支持直接文字输入
- 输入的文字同样会进行智能分类识别

### 5. 编辑和提交
- 识别结果可随时编辑修改
- 修改后会重新进行智能分类
- 点击"提交记录"保存到系统

## 使用流程

1. **进入录音页面**
   - 在首页点击"🎤 语音记录"按钮

2. **开始录音**
   - 点击"点击开始录音"按钮
   - 首次使用需授权麦克风权限
   - 对着麦克风清晰说出要记录的内容

3. **停止录音**
   - 说完后点击"停止录音"按钮
   - 系统会自动进行语音识别

4. **查看识别结果**
   - 识别完成后显示文字内容
   - 显示智能识别的类别、子类别、数量等信息

5. **编辑（可选）**
   - 如果识别有误，可点击文本框进行修改
   - 修改后系统会重新分析

6. **提交记录**
   - 确认无误后点击"提交记录"按钮
   - 记录成功后自动返回首页

## 技术实现

### 使用的技术
- **微信同声传译插件**：用于语音转文字
- **录音管理器**：管理录音过程
- **智能文本分析**：正则表达式匹配关键词
- **状态管理**：React Hooks

### 配置要求

1. **微信小程序插件配置**
   在 `app.config.ts` 中已配置：
   ```typescript
   plugins: {
     WechatSI: {
       version: '0.3.5',
       provider: 'wx069ba97219f66d99',
     },
   }
   ```

2. **权限配置**
   在 `app.config.ts` 中已配置录音权限：
   ```typescript
   permission: {
     'scope.record': {
       desc: '需要使用您的麦克风进行语音识别',
     },
   }
   ```

3. **微信公众平台配置**
   - 登录[微信小程序后台](https://mp.weixin.qq.com/)
   - 进入"设置" → "第三方设置" → "插件管理"
   - 添加插件：**微信同声传译** (AppID: `wx069ba97219f66d99`)

## 文件结构

```
src/
├── pages/
│   ├── voice-record/
│   │   ├── index.tsx          # 语音识别页面主文件
│   │   └── index.less         # 样式文件
│   └── home/
│       ├── index.tsx          # 首页（已添加语音入口按钮）
│       └── index.less         # 样式文件
├── utils/
│   └── api.ts                 # API接口（包含创建记录接口）
└── app.config.ts              # 应用配置（已添加插件和权限配置）
```

## 注意事项

1. **麦克风权限**
   - 首次使用需要用户授权麦克风权限
   - 如果用户拒绝，可引导用户在设置中开启

2. **网络要求**
   - 语音识别需要网络连接
   - 建议在 Wi-Fi 环境下使用以获得更好体验

3. **录音环境**
   - 在安静的环境下录音效果更好
   - 说话时请靠近麦克风，语速适中，吐字清晰

4. **识别准确性**
   - 普通话识别率较高
   - 方言可能识别不准确，建议使用手动输入
   - 识别结果可随时编辑修改

5. **兼容性**
   - 仅支持微信小程序环境
   - 需要微信版本 7.0.0 及以上

## 未来优化方向

1. **识别准确性提升**
   - 添加更多关键词规则
   - 支持自定义关键词
   - 机器学习优化分类算法

2. **用户体验优化**
   - 添加录音波形显示
   - 支持多语言识别
   - 历史记录快速重复

3. **功能扩展**
   - 支持语音编辑记录
   - 支持语音搜索记录
   - 添加语音提醒功能

## 问题排查

### 无法录音
1. 检查麦克风权限是否已授权
2. 检查设备是否支持录音功能
3. 尝试重启小程序

### 识别失败
1. 检查网络连接
2. 确认是否添加了微信同声传译插件
3. 使用手动输入功能作为备选方案

### 分类识别不准确
1. 可在识别后手动编辑文本
2. 重新编辑后会自动重新分类
3. 或直接使用首页的分类按钮进行记录

## 联系支持

如有问题或建议，请联系开发团队。
